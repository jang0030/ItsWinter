<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="mobilesdkdemo.rbbn.itswinter">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
      
        <activity android:name=".recipe.activity_recipe_favorites_list"></activity>
        <activity android:name=".recipe.activity_recipe_page" />

        <activity android:name=".event.eventDetails"></activity>
        <activity android:name=".eventResults" />
        <!-- <activity android:name=".EventListViewLayout" /> -->
        <activity android:name=".HelpActivity" />

        <activity android:name=".covid.CovidResultActivity"></activity>
        <activity android:name=".audio.MyAlbumActivity" />
        <activity android:name=".audio.AlbumDetailActivity" />

        <activity android:name=".event.EventHomeActivity" />
        <activity android:name=".recipe.RecipeHomeActivity" />
        <activity android:name=".covid.CovidHomeActivity" />
        <activity android:name=".audio.AudioHomeActivity" />
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>